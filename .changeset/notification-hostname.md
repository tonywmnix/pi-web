---
"@jmfederico/pi-web": minor
---

Name the machine in desktop notifications, so a gateway fronting more than one says which host a run finished or blocked on. The local machine now reports its hostname alongside the fixed "Local" display name, and a remote falls back to the name it was added under.
