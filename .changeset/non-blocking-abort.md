---
"@jmfederico/pi-web": patch
---

Answer session abort requests as soon as the abort is issued instead of holding the connection until the turn unwinds; completion is reported through session status and activity.
