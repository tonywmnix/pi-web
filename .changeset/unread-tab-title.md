---
"@jmfederico/pi-web": minor
---

Show the unread session count in the browser tab title, as `(3) PI WEB`, so a tab you have switched away from says how much is waiting. The count leads so it survives tab truncation, caps at `99+`, and disappears once everything is read.
